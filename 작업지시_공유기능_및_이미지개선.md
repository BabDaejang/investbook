# 작업 지시서: 표지 이미지 고화질화 + 공유 버튼 + OG 메타태그

너는 아래 3가지 작업을 순서대로 수행한다. 이 문서에 있는 코드는 그대로 복사해서 사용하고, 지시하지 않은 파일은 절대 수정하지 마라.

## 프로젝트 정보 (먼저 읽을 것)

- 위치: `E:\Python\vscode\investbook`
- 스택: Next.js 16 (App Router) + TypeScript + Tailwind CSS + Prisma(PostgreSQL) + @tanstack/react-query + sonner(토스트) + lucide-react(아이콘)
- 도서 검색은 카카오 도서검색 API를 사용한다. 관련 코드는 `src/lib/kakao.ts`에 있고, 검색 결과를 `NormalizedBook` 형태로 정규화한다.
- DB의 `Book` 모델에는 `thumbnail`(표지 URL), `naverUrl`(도서 페이지 URL — 과거 호환을 위해 필드명을 유지 중), `authors`(JSON 문자열 배열) 필드가 있다.
- 배포 주소: `https://investbook-sage.vercel.app`
- 사이트 이름: "세종 금융경제교육 교사연구회 도서 탐색기"
- `public/og-image.png` (1200×630 대표 이미지)가 **이미 존재한다.** 새로 만들지 마라.

### 절대 하지 말 것

- `prisma migrate`, `prisma db push` 등 DB 스키마 변경 명령 실행 금지. `prisma/schema.prisma` 수정 금지.
- `naverUrl` 필드/컬럼 이름 변경 금지 (DB 호환용으로 유지 중이다).
- `src/lib/kakao.ts`의 `searchBooks` 정규화 로직 변경 금지 — 아래 지시된 `thumbnail` 한 줄만 수정한다.
- `_to_delete` 폴더는 건드리지 마라.
- `.env`의 기존 줄 삭제/수정 금지 — 새 줄 추가만 허용.
- 새 npm 패키지 설치 금지. 지금 있는 것만으로 전부 가능하다.

---

## 작업 1. 표지 이미지 고화질화

**배경**: 카카오 API의 `thumbnail`은 `https://search1.kakaocdn.net/thumb/R120x174.q85/?fname=<원본URL>` 형태의 120×174 축소판이다. URL의 `fname` 쿼리 파라미터에 원본 고화질 이미지 주소가 들어 있으므로, 이것을 꺼내 쓰면 된다. 이미 DB에 저장된 저화질 책도 화면에서 자동으로 고화질이 되도록 **렌더링 시점에 변환**한다 (DB는 건드리지 않는다).

### 1-1. 새 파일 `src/lib/bookImage.ts` 생성 (아래 코드 그대로)

```ts
/**
 * 카카오 썸네일 URL(저해상도 축소판)을 원본 고해상도 이미지 URL로 변환한다.
 * - kakaocdn 썸네일이면 fname 파라미터의 원본 URL을 반환 (http는 https로 교체)
 * - 그 외 URL(네이버 pstatic 등)은 그대로 반환
 */
export function getHighResThumbnail(url?: string | null): string {
  if (!url) return '';
  try {
    const u = new URL(url);
    if (u.hostname.endsWith('kakaocdn.net') && u.pathname.startsWith('/thumb/')) {
      const fname = u.searchParams.get('fname');
      if (fname) {
        return fname.replace(/^http:\/\//, 'https://');
      }
    }
  } catch {
    // URL 파싱 실패 시 원본 그대로 반환
  }
  return url;
}
```

### 1-2. `src/lib/kakao.ts` 수정 (딱 두 군데)

파일 맨 위에 import 추가:

```ts
import { getHighResThumbnail } from '@/lib/bookImage';
```

`searchBooks` 안의 `return { ... }` 객체에서 `thumbnail: item.thumbnail || '',` 줄을 찾아 아래로 교체:

```ts
      thumbnail: getHighResThumbnail(item.thumbnail),
```

### 1-3. 화면 컴포넌트에서 썸네일 표시 부분을 헬퍼로 감싸기

아래 6개 파일 각각에서 `<Image src={...}>`(next/image)에 thumbnail이 들어가는 곳을 찾아, `src` 값만 `getHighResThumbnail(...)`로 감싼다. 각 파일 상단에 `import { getHighResThumbnail } from '@/lib/bookImage';`도 추가한다.

1. `src/components/book/BookCard.tsx`
2. `src/components/book/BookSearchResult.tsx`
3. `src/components/book/BookDetailPanel.tsx`
4. `src/components/layout/RightSidebar.tsx`
5. `src/app/books/[id]/page.tsx`
6. `src/app/search/page.tsx`

예시 — 이런 코드가 있으면:

```tsx
{book.thumbnail && <Image src={book.thumbnail} alt={book.title} fill className="object-cover" sizes="112px" />}
```

이렇게 바꾼다 (**조건문 `book.thumbnail &&`는 그대로 두고 `src`만 감싼다**):

```tsx
{book.thumbnail && <Image src={getHighResThumbnail(book.thumbnail)} alt={book.title} fill className="object-cover" sizes="112px" />}
```

각 파일에 thumbnail을 그리는 곳이 여러 개일 수 있으니 `thumbnail`로 파일 내 검색해서 `<Image>`의 `src`에 쓰이는 곳을 전부 처리하라.

**참고**: `next.config.ts`에는 `*.kakaocdn.net`, `*.daumcdn.net`, `shopping-phinf.pstatic.net`이 이미 이미지 허용 도메인으로 등록되어 있다. 만약 실행 중 `next/image`가 "hostname is not configured" 에러를 내면, 에러 메시지에 나온 hostname을 `next.config.ts`의 `remotePatterns`에 같은 형식으로 추가하라.

---

## 작업 2. 도서 상세 페이지에 공유 버튼 (링크 복사 + 카카오톡 공유)

### 2-1. `.env` 파일에 아래 두 줄 추가 (기존 내용은 절대 건드리지 말 것)

```
NEXT_PUBLIC_SITE_URL=https://investbook-sage.vercel.app
NEXT_PUBLIC_KAKAO_JS_KEY=여기에_카카오_JavaScript_키
```

`NEXT_PUBLIC_KAKAO_JS_KEY` 값은 네가 알 수 없다. 사용자에게 "카카오 개발자 콘솔(developers.kakao.com) → 내 애플리케이션 → 앱 키 → **JavaScript 키**를 알려달라"고 요청하고, 받아서 넣어라. (REST API 키와 다른 키다. `.env`에 이미 있는 `KAKAO_REST_API_KEY`를 복사해 넣으면 안 된다.)

### 2-2. `src/app/layout.tsx`에 카카오 SDK 스크립트 추가

파일 상단에 import 추가:

```tsx
import Script from 'next/script';
```

`<body ...>` 태그 바로 안쪽(children 위)에 아래 한 줄 추가:

```tsx
        <Script src="https://t1.kakaocdn.net/kakao_js_sdk/2.7.4/kakao.min.js" strategy="afterInteractive" />
```

### 2-3. 새 파일 `src/components/book/ShareButtons.tsx` 생성 (아래 코드 그대로)

```tsx
'use client';

import { Button } from '@/components/ui/button';
import { toast } from 'sonner';
import { Link as LinkIcon, MessageCircle } from 'lucide-react';

interface ShareButtonsProps {
  title: string;
  description?: string;
  thumbnail?: string;
}

export function ShareButtons({ title, description, thumbnail }: ShareButtonsProps) {
  const copyLink = async () => {
    try {
      await navigator.clipboard.writeText(window.location.href);
      toast.success('링크가 복사되었습니다.');
    } catch {
      toast.error('링크 복사에 실패했습니다.');
    }
  };

  const shareKakao = () => {
    const Kakao = (window as any).Kakao;
    const jsKey = process.env.NEXT_PUBLIC_KAKAO_JS_KEY;
    if (!Kakao || !jsKey) {
      toast.error('카카오톡 공유를 사용할 수 없습니다. (SDK 또는 키 미설정)');
      return;
    }
    if (!Kakao.isInitialized()) {
      Kakao.init(jsKey);
    }
    const url = window.location.href;
    Kakao.Share.sendDefault({
      objectType: 'feed',
      content: {
        title,
        description: description || '',
        imageUrl: thumbnail || `${window.location.origin}/og-image.png`,
        link: { mobileWebUrl: url, webUrl: url },
      },
      buttons: [
        { title: '자세히 보기', link: { mobileWebUrl: url, webUrl: url } },
      ],
    });
  };

  return (
    <div className="grid grid-cols-2 gap-1.5">
      <Button size="sm" variant="outline" className="w-full text-slate-600 border-slate-200 hover:bg-slate-50 text-xs" onClick={copyLink}>
        <LinkIcon className="w-3 h-3 mr-1" /> 링크 복사
      </Button>
      <Button size="sm" className="w-full bg-[#FEE500] hover:bg-[#FEE500]/90 text-[#191919] border-none text-xs" onClick={shareKakao}>
        <MessageCircle className="w-3 h-3 mr-1" /> 카톡 공유
      </Button>
    </div>
  );
}
```

### 2-4. `src/app/books/[id]/page.tsx`에 버튼 배치

파일 상단에 import 추가:

```tsx
import { ShareButtons } from '@/components/book/ShareButtons';
```

이 파일 안에서 `알라딘` 버튼(아래와 비슷한 코드)을 찾아라:

```tsx
                  <Button size="sm" variant="outline" ...
                    onClick={() => window.open(`https://www.aladin.co.kr/search/wsearchresult.aspx?SearchWord=${book.isbn || book.title}`, '_blank')}>
                    알라딘
                  </Button>
```

알라딘/다음 책/YES24/교보문고 버튼들을 감싸고 있는 컨테이너 `<div>` **바로 위**에 아래를 추가한다:

```tsx
                <ShareButtons
                  title={book.title}
                  description={[parsedAuthors, book.publisher].filter(Boolean).join(' · ')}
                  thumbnail={getHighResThumbnail(book.thumbnail)}
                />
```

`parsedAuthors`는 저자 이름 문자열이다. 이 페이지에 이미 `book.authors`(JSON 문자열)를 파싱해서 쓰는 변수가 있으면 그것을 재사용하라. 없으면 컴포넌트 함수 안(return 위)에 아래를 추가해서 사용하라:

```tsx
  let parsedAuthors = '';
  try {
    parsedAuthors = JSON.parse(book.authors || '[]').join(', ');
  } catch {
    parsedAuthors = '';
  }
```

주의: 이 페이지에서 `book`이 로딩 전 undefined일 수 있으니, 기존 코드가 book 존재를 확인한 이후 위치(다른 버튼들이 렌더링되는 곳)에 넣으면 안전하다.

---

## 작업 3. OG 메타태그 (공유 시 미리보기 이미지)

### 3-1. `src/app/layout.tsx`의 `metadata`를 아래로 교체

기존에 이런 코드가 있다:

```tsx
export const metadata: Metadata = {
  title: "세종 금융경제교육 교사연구회 도서 탐색기",
  description: "금융 및 경제 도서를 체계적으로 탐색하고 큐레이션하는 플랫폼",
};
```

아래로 교체한다 (title/description 문구는 유지):

```tsx
export const metadata: Metadata = {
  metadataBase: new URL(process.env.NEXT_PUBLIC_SITE_URL || 'https://investbook-sage.vercel.app'),
  title: {
    default: "세종 금융경제교육 교사연구회 도서 탐색기",
    template: "%s | 세종 금융경제교육 교사연구회 도서 탐색기",
  },
  description: "금융 및 경제 도서를 체계적으로 탐색하고 큐레이션하는 플랫폼",
  openGraph: {
    type: "website",
    siteName: "세종 금융경제교육 교사연구회 도서 탐색기",
    title: "세종 금융경제교육 교사연구회 도서 탐색기",
    description: "금융 및 경제 도서를 체계적으로 탐색하고 큐레이션하는 플랫폼",
    images: [{ url: "/og-image.png", width: 1200, height: 630 }],
  },
};
```

### 3-2. 새 파일 `src/app/books/[id]/layout.tsx` 생성 (아래 코드 그대로)

도서 상세 페이지(`page.tsx`)는 클라이언트 컴포넌트라서 metadata를 내보낼 수 없으므로, 같은 경로에 서버 컴포넌트 layout을 만들어 책마다 다른 OG 태그를 생성한다.

**중요**: 이 파일에 `'use client'`를 절대 붙이지 마라. 그리고 Next.js 16에서는 `params`가 Promise라서 반드시 `await` 해야 한다 — 아래 코드가 이미 그렇게 되어 있으니 그대로 써라.

```tsx
import type { Metadata } from 'next';
import { prisma } from '@/lib/prisma';
import { getHighResThumbnail } from '@/lib/bookImage';

export async function generateMetadata(
  { params }: { params: Promise<{ id: string }> }
): Promise<Metadata> {
  const { id } = await params;

  try {
    const book = await prisma.book.findUnique({ where: { id } });
    if (!book) return {};

    let authors = '';
    try {
      authors = JSON.parse(book.authors || '[]').join(', ');
    } catch {
      authors = '';
    }

    const description =
      [authors, book.publisher].filter(Boolean).join(' · ') ||
      book.description?.slice(0, 100) ||
      '도서 상세 정보';
    const image = book.thumbnail ? getHighResThumbnail(book.thumbnail) : '/og-image.png';

    return {
      title: book.title,
      description,
      openGraph: {
        type: 'article',
        title: book.title,
        description,
        images: [{ url: image }],
      },
    };
  } catch {
    return {};
  }
}

export default function BookDetailLayout({ children }: { children: React.ReactNode }) {
  return children;
}
```

---

## 완료 후 검증 (반드시 순서대로 실행하고 결과를 보고할 것)

1. `npm run dev`로 실행하고 컴파일 에러가 없는지 확인한다.
2. 검색 페이지에서 아무 책이나 검색 → 표지가 선명하게(고화질) 나오는지 확인한다.
3. 최근에 추가되어 표지가 깨져 보이던 기존 책도 이제 선명하게 나오는지 확인한다.
4. 도서 상세 페이지에서 "링크 복사" 클릭 → "링크가 복사되었습니다" 토스트가 뜨는지 확인한다.
5. "카톡 공유" 클릭 → 카카오톡 공유 창이 열리는지 확인한다. (localhost에서 안 되면 아래 "사람이 할 일" 3번의 도메인 등록이 안 된 것이다.)
6. 페이지 소스 보기(또는 curl)로 도서 상세 페이지 `<head>`에 `og:title`, `og:image` 태그가 책 정보로 채워져 있는지 확인한다.

## 사람(사용자)이 직접 해야 할 일 — 작업 완료 후 사용자에게 아래를 안내하라

1. 카카오 개발자 콘솔 → 내 애플리케이션 → 앱 키에서 **JavaScript 키**를 복사해 `.env`의 `NEXT_PUBLIC_KAKAO_JS_KEY`에 넣기 (작업 중에 이미 받았다면 생략).
2. Vercel 대시보드 → 프로젝트 → Settings → Environment Variables에 `NEXT_PUBLIC_KAKAO_JS_KEY`와 `NEXT_PUBLIC_SITE_URL`(값: `https://investbook-sage.vercel.app`) 추가 후 재배포.
3. 카카오 개발자 콘솔 → 내 애플리케이션 → 플랫폼 → Web에 사이트 도메인 두 개 등록: `http://localhost:3000`, `https://investbook-sage.vercel.app` (등록해야 카카오톡 공유가 동작한다).
4. 배포 후 카카오톡 미리보기 이미지가 예전 것으로 나오면: https://developers.kakao.com/tool/debugger/sharing 에서 페이지 URL을 넣고 "초기화"로 카카오 OG 캐시를 갱신.
